<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
  <channel>
    <title>{{ site.title }}</title>
    <link>{{ site.url }}</link>
    <description>{{ site.description }}</description>
    <lastBuildDate>{{ posts[0].date.strftime('%a, %d %b %Y %H:%M:%S +0000') }}</lastBuildDate>
    <language>en-us</language>

    {% for post in posts %}
    <item>
      <title>{{ post.meta.title }}</title>
      <link>{{ site.url }}/posts/{{ post.meta.slug }}</link>
      <description><![CDATA[{{ post.meta.content | safe }}]]></description>
      <pubDate>{{ post.date.strftime('%a, %d %b %Y %H:%M:%S +0000') }}</pubDate>
      <guid>{{ site.url }}/posts/{{ post.meta.slug }}</guid>
    </item>
    {% endfor %}
  </channel>
</rss>
